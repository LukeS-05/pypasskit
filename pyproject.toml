[build-system]
requires = ["hatchling >= 1.26"]
build-backend = "hatchling.build"

[project]
name = "pypasskit"
version = "0.6.0"
description = ""
readme = "README.md"
requires-python = ">=3.8"
dependencies = []